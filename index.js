// code your solution here
let saturdayFun = (satFun = 'roller-skate') => {
    return `This Saturday, I want to ${satFun}!`
}

let mondayWork = (monWork = 'go to the office') => {
    return `This Monday, I will ${monWork}.`
}

let wrapAdjective = () => {
    let innerAdjective = (innerAdj = "special") => {
        return `You are ${innerAdj}`
        let secondIn = (secInn = "*") => {
            wrapAdjective("%")("A dedicated programmer");
        }
    }
}